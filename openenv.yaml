# openenv.yaml — AI Conveyor Color Segregation System
# =====================================================
# IMPORTANT: Do NOT add &tls=true to the URI.
# The app uses certifi to handle TLS correctly for Python 3.14 + Atlas.

# ── Server ──────────────────────────────────────────────────────
FASTAPI_PORT: 8000

# ── MongoDB Atlas ───────────────────────────────────────────────
# Remove &tls=true — certifi handles SSL in code
MONGO_URI: "mongodb+srv://dhanyags13_db_user:pCksMQIAvnUxYaXp@cluster0.uv5ndj8.mongodb.net/conveyor_db?retryWrites=true&w=majority"
MONGO_DB: "conveyor_db"

# ── JWT ─────────────────────────────────────────────────────────
# Generate real key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY: "ai_conveyor_secret_key_change_in_production_2024"
JWT_EXPIRE_HOURS: 8

# ── Debug ────────────────────────────────────────────────────────
DEBUG: "true"

# ── Built-in accounts (seeded at startup) ────────────────────────
# admin / admin123
# demo  / demo